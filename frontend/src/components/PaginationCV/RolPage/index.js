import React from "react";
import PropTypes from "prop-types";
import { Text, View } from "react-native";
import style from "./style";

const RolPage = (props) => {
  return (
    <View>
      <Text>Pagina de RolPage</Text>
    </View>
  );
};

RolPage.propTypes = {};

export default RolPage;
